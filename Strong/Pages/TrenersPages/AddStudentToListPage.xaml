<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Strong.Pages.TrenersPages.AddStudentToListPage"
             Title="AddStudentToListPage">

    <StackLayout Orientation="Vertical" Padding="5">

        <Label Text="свободные ученики:" HorizontalTextAlignment="Center" Margin="5"/>

        <CollectionView ItemsSource="{Binding StudentsList}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Label Text="{Binding UserName}" FontSize="18" VerticalOptions="Center" />
                        <Button Text="доавить" Command="{Binding AddStudentForTrainerCommand}"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="назад" Command="{Binding BackCommand}" />

    </StackLayout>
        
        
</ContentPage>