<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Strong.Pages.StudentsPages.AboutTrainingPage"
             Title="AboutTrainingPage">

    <ScrollView>
        <StackLayout Orientation="Vertical">


            <CollectionView ItemsSource="{Binding SetsList}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding exercise_id}" VerticalTextAlignment="Center" Margin="5"/>

                            <StackLayout Orientation="Horizontal">
                                <Entry Text="{Binding exercise_weight , Mode=TwoWay}" Margin="5"/>
                                <Entry Text="{Binding exercise_reps  , Mode=TwoWay}" Margin="5"/>
                                <Entry Text="{Binding rest_time , Mode=TwoWay}" Margin="5"/>
                            </StackLayout>

                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ScrollView>
</ContentPage>